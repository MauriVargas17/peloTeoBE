
/*import { Router } from 'express';
import { RecoveryController } from '../controllers/recoveryControllers';
export function recoveryRoutes(recoveryController: RecoveryController): Router {
    const router = Router();

    router.post('/', recoveryController.crea);
    
    return router;
}
*/